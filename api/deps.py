# api/deps.py
from typing import List, Dict

# Nesta fase, simulamos dataset em memória; depois leremos do CSV.
def get_dataset() -> List[Dict]:
    return []  # placeholder; será populado quando o CSV existir
